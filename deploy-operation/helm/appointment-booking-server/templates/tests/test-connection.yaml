apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appointment-booking-server.fullname" . }}-test-connection"
  labels:
    {{- include "appointment-booking-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appointment-booking-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
