apiVersion: v1
kind: Service
metadata:
  name: {{ include "appointment-booking-server.fullname" . }}
  labels:
    {{- include "appointment-booking-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appointment-booking-server.selectorLabels" . | nindent 4 }}
